on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    name: say hello
    steps:
    - name: wow a step
      id: helloo
      uses: actions/update-cloudflare-dns-records@v0.2
      with:
        zone: ね.co
    - name: step 2
      run: echo sure

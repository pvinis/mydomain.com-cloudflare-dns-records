name: Update Cloudflare DNS records

on:
  push:
    branches:
      - master
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    name: Update Cloudflare DNS records
    steps:
    - name: Update Cloudflare DNS records
      uses: pvinis/update-cloudflare-dns@v0.2.1
      with:
        zone: ね.co

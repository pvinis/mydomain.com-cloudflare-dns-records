name: main workflow

on:
  push:
    branches:
      - master
      - main


jobs:
  hello:
    runs-on: ubuntu-latest
    name: say hello
    steps:
    - name: wow a step
      id: helloo
      uses: pvinis/update-cloudflare-dns@v0.2
      with:
        zone: ね.co
    - name: step 2
      run: echo sure
